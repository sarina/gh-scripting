### bash scripts to automate bulk github actions

`depr-*`: scripts to copy the depr workflow (present in openedx/.github) across
every org repository and open up a PR for the change; immediately merges the PRs. 